# Public browser configuration only. Never place secrets, database URLs, or provider keys here.
# Fixture mode is the recommended portfolio/demo default.
NEXT_PUBLIC_DATA_MODE=mock
# Required only when NEXT_PUBLIC_DATA_MODE=live; use a verified, public API origin.
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
