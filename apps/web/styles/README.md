# Styles

Global tokens and base styles are intentionally maintained in `app/globals.css` so the App Router
root layout imports them once. Shared non-global styles can be added here when a future feature needs
them.
