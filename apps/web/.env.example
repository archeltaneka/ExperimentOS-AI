# Explicitly choose the deterministic portfolio data layer or the live /ask adapter.
NEXT_PUBLIC_DATA_MODE=mock
# Required only when NEXT_PUBLIC_DATA_MODE=live.
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
